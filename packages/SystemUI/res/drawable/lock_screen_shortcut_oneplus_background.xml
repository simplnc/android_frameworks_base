<?xml version="1.0" encoding="utf-8"?>
<!-- OnePlus-inspired lock screen shortcut background -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="oval">
            <solid android:color="@color/lock_screen_shortcut_press" />
            <stroke 
                android:width="2dp" 
                android:color="@color/lock_screen_clock_oneplus_primary" />
            <size android:width="56dp" android:height="56dp" />
        </shape>
    </item>
    
    <!-- Hover state -->
    <item android:state_hovered="true">
        <shape android:shape="oval">
            <solid android:color="@color/lock_screen_shortcut_hover" />
            <stroke 
                android:width="2dp" 
                android:color="@color/lock_screen_clock_oneplus_primary" />
            <size android:width="56dp" android:height="56dp" />
        </shape>
    </item>
    
    <!-- Default state -->
    <item>
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent" />
            <stroke 
                android:width="2dp" 
                android:color="@color/lock_screen_clock_oneplus_primary" />
            <size android:width="56dp" android:height="56dp" />
        </shape>
    </item>
</selector>
